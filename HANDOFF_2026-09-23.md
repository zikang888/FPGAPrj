# Zynq-7020 多协议平台开发交接说明

> 交接日期：2026-09-23  
> 当前唯一正式工程：`D:\fpga_class\Multi_protocol`  
> 当前状态：FPGA 全量实现通过，PS 软件构建通过，LCD/GT911/事件快照链路已启动；D1 管脚修正后已在实板确认闪烁。

## 1. 接手时只使用这些路径

正式工程根目录：

```text
D:\fpga_class\Multi_protocol
```

当前正式上板产物：

```text
D:\fpga_class\Multi_protocol\artifacts\current\multi_protocol_bd_wrapper.bit
D:\fpga_class\Multi_protocol\artifacts\current\multi_protocol_bd_wrapper.hdf
D:\fpga_class\Multi_protocol\artifacts\current\platform_app.elf
```

当前正式 SDK 工作区：

```text
D:\fpga_class\Multi_protocol\artifacts\current\sdk_workspace_d1
```

以下目录仅用于参考或历史回退，不是当前生产工程：

```text
D:\fpga_class\oldtouch_ui\touch_ui
D:\fpga_class\touch_ui-7
D:\fpga_class\QRS_fpga
D:\fpga_class\Multi_protocol\artifacts\current\sdk_workspace
D:\fpga_class\Multi_protocol\Multi_protocol.runs\impl_1
```

特别注意：`artifacts\current\sdk_workspace` 是 D1 管脚修正前的旧工作区，当前一键下载脚本已经切换到 `sdk_workspace_d1`，不要混用。

## 2. 当前已经实现的功能

### 2.1 PL/PS 基础平台

- Zynq-7020，器件型号 `xc7z020clg484-2`。
- PS 通过 AXI4-Lite 访问自定义 `multi_protocol_core`，基地址为 `0x40000000`。
- AXI scratch 寄存器读写已通过。
- LCD 使用 800×480 RGB565 输出，VDMA + VTC 驱动。
- GT911 使用 PS I2C0，地址 `0x14`。
- GT911 触摸中断使用 MIO0；复位使用 EMIO0，对应封装管脚 R15。
- UART 使用 COM10，波特率 115200。

### 2.2 事件快照演示链路

- 统一事件宽度：128 bit。
- 快照缓冲深度：256 条。
- 触发后保留事件数：16 条。
- 当前虚拟事件闭环的正确结果：
  - snapshot ID：1
  - event count：25
  - trigger index：8
  - dropped：0
- 数据链路：虚拟事件源 → PL 快照缓冲 → AXI 寄存器 → PS 软件 → LCD 显示。

### 2.3 LCD 与触摸

当前程序启动成功时，COM10 应输出：

```text
PS READY
PL ID OK: 0x4D505254
SCRATCH OK: 0xA5A55A5A
GT911 ID: 911
GT911 cfg ver: 0x44, report=10ms
GT911 IRQ ready, pin=MIO0 mode=1
GT911 init done, addr=0x14
TOUCH READY
CAPTURE READY: id=1 count=25 trigger=8 dropped=0
UI READY
```

LCD 已在实板显示成功。此前出现“只有背光、无画面”并非 LCD 逻辑失效，而是只下载了 BIT、没有执行 PS 初始化并下载 ELF。必须使用完整 SDK 流程或一键脚本。

屏幕底部中央有 `PL LED CONTROL` 按钮：

- 显示区域约为 `x=250..549, y=390..459`。
- 软件有效命中区域为 `x=250..549, y=350..459`。
- 每次按下并松开切换一次 `LED_CTRL.bit0`。
- 屏幕文字在 `ON` / `OFF` 之间切换。
- 串口输出 `LED ON` / `LED OFF`。

## 3. D0/D1 管脚结论——务必保留

此前错误地把 PS 可控 LED 约束到 P15。实板测试表明 P15 不是本载板上的 D1，因此当时始终只能看到 D0 自己闪烁。

当前已修正为：

| 板载 LED | FPGA 管脚 | 当前用途 |
|---|---|---|
| D0 | P20 | PL 心跳灯，持续独立闪烁 |
| D1 | P21 | `LED_CTRL.bit0` 控制的 LED |
| D2 | R20 | 当前正式工程未使用 |

正式约束文件：

```text
D:\fpga_class\Multi_protocol\fpga\constraints\top.xdc
```

当前关键约束：

```tcl
set_property -dict {PACKAGE_PIN P20 IOSTANDARD LVCMOS33} [get_ports led_heartbeat]
set_property -dict {PACKAGE_PIN P21 IOSTANDARD LVCMOS33} [get_ports led_ps_active]
```

D1 控制寄存器：

```text
地址：0x40000030
bit0：D1 控制位
```

PS 软件写寄存器的位置：

```text
D:\fpga_class\Multi_protocol\software\src\main.c
```

PL 寄存器与输出连接的位置：

```text
D:\fpga_class\Multi_protocol\fpga\rtl\top\multi_protocol_core.v
```

D1 修正后的实板强制闪烁测试已经通过：D1 按 0.5 秒亮、0.5 秒灭的节奏闪烁 8 次。用户已亲眼确认 D1 闪烁。D0 同时保持自己的心跳节奏，属于正常现象。

需要继续确认的一点：D1 修正后，用户尚未反馈屏幕按钮的最终目视结果。代码路径与强制寄存器测试均已通过，接手后应首先让用户点击一次屏幕底部按钮，确认 D1、屏幕 `ON/OFF` 和串口 `LED ON/OFF` 三者同步。

## 4. 当前正式构建结果

2026-09-23 D1/P21 修正版完整实现结果：

| 检查项 | 结果 |
|---|---:|
| WNS | +3.766 ns |
| TNS | 0.000 ns |
| WHS | +0.030 ns |
| THS | 0.000 ns |
| 时序约束 | 全部满足 |
| DRC Error | 0 |
| DRC Advisory | 2 |

两条 advisory 为 VDMA BRAM 的 `REQP-165` 和 `REQP-181`，与修正前一致，不是 D1 修改引入的问题。

正式报告：

```text
D:\fpga_class\Multi_protocol\artifacts\current\reports\timing.rpt
D:\fpga_class\Multi_protocol\artifacts\current\reports\utilization.rpt
D:\fpga_class\Multi_protocol\artifacts\current\reports\drc.rpt
```

当前正式产物 SHA-256：

| 文件 | 字节数 | SHA-256 |
|---|---:|---|
| `multi_protocol_bd_wrapper.bit` | 4,045,685 | `9B6CACD7A0E4970F37265C72026DDEF76A0E0ED32A14745323716840195969B2` |
| `multi_protocol_bd_wrapper.hdf` | 377,443 | `5D61B34B54CE9CF51785059E493CCA79B1ED98FEEA212F5A872154D4716A99F4` |
| `platform_app.elf` | 738,736 | `1941CF75E4032030FE14C22FCF112FFCE01DBBDF597BEFE26F076B0A31ECFDA4` |

清单文件：

```text
D:\fpga_class\Multi_protocol\artifacts\current\MANIFEST.md
```

## 5. 推荐的一键上板流程

板卡上电、JTAG 和串口连接后，在 PowerShell 中执行：

```powershell
$env:PROCESSOR_ARCHITECTURE='AMD64'
$env:PROCESSOR_ARCHITEW6432='AMD64'
& 'D:\Vivado2018.3\SDK\2018.3\bin\xsct.bat' 'D:\fpga_class\Multi_protocol\download_jtag.tcl'
```

此脚本会完成：

1. 连接 JTAG。
2. 下载当前正式 BIT。
3. 加载当前 HDF。
4. 执行 `ps7_init` 和 `ps7_post_config`。
5. 复位 ARM Cortex-A9 #0。
6. 下载当前 ELF。
7. 从 `0x00100000` 启动程序。

成功时应出现：

```text
Successfully downloaded D:/fpga_class/Multi_protocol/artifacts/current/platform_app.elf
Setting PC to Program Start Address 0x00100000
```

## 6. SDK GUI 上板流程

SDK 2018.3 中使用以下工作区：

```text
D:\fpga_class\Multi_protocol\artifacts\current\sdk_workspace_d1
```

Project Explorer 中应出现：

```text
platform_hw
platform_bsp
platform_app
```

操作顺序：

1. `Xilinx` / `Xilinx Tools` → `Program FPGA`。
2. BIT 选择 `artifacts\current\multi_protocol_bd_wrapper.bit`。
3. Hardware Platform 选择 `platform_hw`。
4. 等待 FPGA 下载完成。
5. 右键 `platform_app`。
6. 选择 `Run As` → `Launch on Hardware (System Debugger)`。

不要只执行 `Program FPGA` 后就结束；LCD 初始化、帧缓冲、VDMA、GT911 和 UI 主循环都依赖 PS 软件。

## 7. D1 单独测试方法

测试脚本：

```text
D:\fpga_class\Multi_protocol\fpga\build\blink_d1_live_test.tcl
```

运行命令：

```powershell
$env:PROCESSOR_ARCHITECTURE='AMD64'
$env:PROCESSOR_ARCHITEW6432='AMD64'
& 'D:\Vivado2018.3\SDK\2018.3\bin\xsct.bat' 'D:\fpga_class\Multi_protocol\fpga\build\blink_d1_live_test.tcl'
```

该脚本暂停 ARM，交替写 `0x40000030 = 1/0`，每个状态保持 500 ms，共 8 次，然后恢复 ARM。成功输出：

```text
D1_P21_TEST_BEGIN
D1_LEVEL=1
D1_LEVEL=0
...
LED_CTRL_READBACK: 40000030: 00000000
D1_P21_TEST_DONE
```

`blink_p15_live_test.tcl` 仅作为旧命令兼容入口保留，它现在会转调 D1 测试脚本，不代表 P15 仍然有效。

## 8. 重新构建 FPGA

为了避开正在打开的 Vivado GUI 对主工程生成目录的锁定，本次验证使用隔离构建目录：

```text
D:\fpga_class\Multi_protocol\_validation_build
```

完整构建命令：

```powershell
$env:PROCESSOR_ARCHITECTURE='AMD64'
$env:PROCESSOR_ARCHITEW6432='AMD64'
& 'D:\Vivado2018.3\Vivado\2018.3\bin\vivado.bat' `
  -mode batch `
  -source 'D:\fpga_class\Multi_protocol\_validation_build\fpga\build\validate_in_process.tcl'
```

重新构建前要把正式源码变更同步到 `_validation_build` 对应位置。D1 修改时同步的是：

```text
fpga\constraints\top.xdc
```

构建成功标志：

```text
IN_PROCESS_BUILD_DONE
```

隔离构建输出：

```text
D:\fpga_class\Multi_protocol\_validation_build\artifacts\multi_protocol_bd_wrapper.bit
D:\fpga_class\Multi_protocol\_validation_build\artifacts\multi_protocol_bd_wrapper.hdf
```

不要仅凭文件存在就发布；必须同时检查 timing report、DRC、文件时间和哈希。

## 9. 重新构建 PS 软件

构建脚本：

```text
D:\fpga_class\Multi_protocol\software\build_ps_app.tcl
```

建议始终使用新的隔离工作区，避免 SDK GUI 锁定旧目录：

```powershell
$env:MULTI_PROTOCOL_HDF='D:\fpga_class\Multi_protocol\artifacts\current\multi_protocol_bd_wrapper.hdf'
$env:MULTI_PROTOCOL_SDK_WS='D:\fpga_class\Multi_protocol\.sdk_next_validation'
$env:_JAVA_OPTIONS='-Duser.home=D:\fpga_class\Multi_protocol\.xilinx_user'
$env:PROCESSOR_ARCHITECTURE='AMD64'
$env:PROCESSOR_ARCHITEW6432='AMD64'
& 'D:\Vivado2018.3\SDK\2018.3\bin\xsct.bat' `
  'D:\fpga_class\Multi_protocol\software\build_ps_app.tcl'
```

SDK 2018.3 可能先在 Eclipse 构建阶段报告 `make not found in PATH`，当前脚本会随后调用 Xilinx 自带的 `make.exe` 直接完成构建。最终判断标准是：

```text
PS_BUILD_DONE
ELF: ...\platform_app\Debug\platform_app.elf
```

当前 ELF 尺寸统计：

```text
text = 115028
data = 2104
bss  = 25280
```

## 10. 当前已知问题和注意事项

1. D0 一直闪烁是设计行为，不是 D1 控制失败。
2. P15 不是当前板载 D1，不要把 `led_ps_active` 改回 P15。
3. 只下载 BIT 会造成 LCD 只有背光、无 UI；必须完成 PS 初始化并下载 ELF。
4. `Multi_protocol.runs\impl_1` 中的 GUI 生成 BIT 不是当前发布基线，不要与 `artifacts\current` 混用。
5. Vivado/SDK GUI 打开时可能锁定工程或工作区。批量构建优先使用隔离目录。
6. Vivado 可能出现无法写 WebTalk settings 的 critical warning；本次 BIT 仍成功生成。真正的验收标准是 bitgen 成功、0 DRC error 和时序满足。
7. 当前正式下载脚本依赖 `sdk_workspace_d1\platform_hw\ps7_init.tcl`，修改工作区名称时必须同步修改 `download_jtag.tcl`。
8. `artifacts\current\sdk_workspace` 内可能包含一次迁移过程中生成的 `.sdk_d1_validation` 子目录，但它整体仍属于旧工作区，不要作为正式入口。

## 11. 建议接手后的第一轮动作

按以下顺序进行，能最快确认现场状态：

1. 打开 COM10，115200 8N1。
2. 运行 `download_jtag.tcl` 完整重新上板。
3. 确认 LCD 显示、串口出现 `UI READY`。
4. 点击屏幕底部 `PL LED CONTROL`，确认 D1、屏幕 `ON/OFF`、串口日志同步。
5. 若触摸无效，先看串口是否为 `TOUCH READY`，再检查坐标命中，不要先改 FPGA 管脚。
6. 若 D1 不响应，运行 `blink_d1_live_test.tcl`：
   - 若强制闪烁正常，问题在触摸/UI 软件路径；
   - 若强制闪烁也不正常，再检查是否烧错 BIT 或约束是否仍为 P21。
7. 完成这一步后，再继续接入 UART、SPI、I2C、CAN、RS-485 等真实协议事件源。

## 12. 关键源码和文档索引

```text
README.md
fpga\constraints\top.xdc
fpga\rtl\top\multi_protocol_core.v
fpga\rtl\capture\event_snapshot_buffer.v
fpga\build\create_bd.tcl
fpga\build\validate_in_process.tcl
fpga\build\blink_d1_live_test.tcl
software\src\main.c
software\src\platform_ui.c
software\src\touch.c
software\build_ps_app.tcl
download_jtag.tcl
docs\hardware\pin_assignment.md
docs\interfaces\register_map.md
docs\test-results\2026-09-23-platform-baseline.md
docs\test-results\2026-09-23-event-snapshot-closed-loop.md
artifacts\current\MANIFEST.md
```

## 13. 交接结论

当前工程不是“只能让 D0 闪烁”的状态。D0 是固定心跳灯；D1 已正确映射到 P21，并已通过 AXI 寄存器强制闪烁的实板验证。下一位开发者应以 `D:\fpga_class\Multi_protocol` 和 `artifacts\current` 为唯一基线，先完成屏幕按钮对 D1 的最终三联验证，再继续开发后续多协议接入功能。

## 14. 2026-09-23 晚间续开发

已新增 SPI Mode 0 被动监听器第一阶段：

```text
fpga\rtl\protocol\spi_mode0_monitor.sv
fpga\sim\protocol\spi_mode0_monitor_tb.sv
docs\interfaces\spi_mode0_monitor.md
docs\test-results\2026-09-23-spi-mode0-monitor.md
```

Vivado Simulator 2018.3 独立仿真已经通过，覆盖事务开始、全双工字节、正常结束和 4 bit 残帧故障触发。监听器已在仿真中通过统一仲裁器进入快照缓存；`multi_protocol_core` 已提供正式的 `ext_evt_*` ready/valid 入口。当前 BD 暂时将该入口绑低，因此仍未分配真实 SPI 管脚，不能据此宣称已经完成实板 SPI 联调。板级接入前需要单片机侧提供实际管脚、SPI 模式、最高 SCLK、bit order、黄金帧和故障匹配规则。

## 15. 统一外部事件入口候选基线

已完成以下结构：

```text
AXI virtual event --\
                     event_arbiter_2 -> event_snapshot_buffer -> AXI -> PS/LCD
external ready/valid /
```

关键约束：

- 外部 ABI：`ext_evt_valid/ext_evt_ready/ext_evt_data[127:0]/ext_evt_trigger`，
  以及生产者累计丢包输入 `ext_evt_dropped_count[31:0]`。
- 仅在 `valid && ready` 时传输；反压期间生产者必须保持数据和 trigger。
- AXI 虚拟源改为一深度保持队列，不再依赖不可反压的单周期脉冲。
- ARM 生效窗口对事件源反压，避免事件被提前握手而未写入快照。
- 协议编号冻结：VIRTUAL=0、UART=1、SPI=2、I2C=3、CAN=4。
- 核心版本 `0x00010002`；能力寄存器 `0x00000003`。
- `EXT_DROPPED_COUNT` (`0x0038`) 可由 PS 读取外部协议生产者的累计丢包数；
  非零时同时置位 `CAPTURE_STATUS[3]`。

五组干净工程自检均通过：快照缓存、两源公平仲裁、核心 AXI/虚拟/外部端到端、SPI 黄金语料、SPI→仲裁→故障快照。最终实现结果：WNS `+3.365 ns`、WHS `+0.036 ns`、DRC 0 Error；资源为 LUT 4,272、寄存器 7,231、BRAM Tile 5.5、DSP 0。

匹配的候选 BIT/HDF/ELF 位于：

```text
D:\fpga_class\Multi_protocol\artifacts\candidates\2026-09-23-readyvalid
```

该目录的 `MANIFEST.md` 含完整 SHA-256。此候选包尚未上板，不得替换
`artifacts\current`。`artifacts\current` 仍是唯一已上板基线。

构建脚本现在支持通过 `MULTI_PROTOCOL_BUILD_DIR` 指定隔离输出目录，无需复制
或同步第二份源码。主 Vivado GUI 被占用时，应设置该变量后依次运行
`create_project.tcl`、`run_sim.tcl` 和 `validate_in_process.tcl`。
