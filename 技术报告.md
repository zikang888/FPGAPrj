# 无线传输信号模拟系统 — 技术报告

## 项目概述

2024 电赛 C 题无线传输信号模拟系统，基于 ZYNQ7020 + STM32F103 + DAC904 架构。

**功能**：产生 30~40MHz 载波信号，支持 CW（等幅波）和 AM（调幅）两种模式，可模拟多径传输效应（直达信号 SD、多径信号 SM、合成信号 SOut 三路可选输出）。

---

## 硬件架构

```
STM32F103C8T6              ZYNQ7020 (XC7Z020-1CLG400C)       DAC904
┌──────────────┐         ┌─────────────────────────┐      ┌─────────┐
│ OLED 128×64  │         │ PS (Cortex-A9)          │      │ 14-bit  │
│ 3 按键       │         │  UART1 ← ASCII 帧解析    │      │ 125MSPS │
│ 菜单/参数    │         │  Xil_Out32 → AXI GPIO   │      │         │
│              │  UART   │                         │ 14bit│         │
│ UART1_TX ────┼─115200─→│ MIO_9 (UART1_RX)       │─────→│ DAC_OUT │
│              │         │                         │      │         │
│              │         │ PL:                     │      │         │
│ GND ─────────┼────────→│  sig_gen_top            │      └─────────┘
└──────────────┘         │   ├ DDS ×3 (LUT查表)     │
                         │   ├ AM 调制 (DSP48E1)    │
                         │   ├ 延时线 (移位寄存器)   │
                         │   ├ 衰减控制 (DSP48E1)    │
                         │   ├ 合路器 (饱和加法)     │
                         │   ├ 输出 MUX (三选一)     │
                         │   └ DAC904 接口          │
                         │                         │
                         │ clk_wiz: 50MHz→125MHz    │
                         └─────────────────────────┘
```

**关键引脚**：

| 连接 | 从 | 到 |
|------|:--|:--|
| 参数帧 | STM32 PA9 (UART1_TX) | Zynq MIO_9 (UART1_RX) |
| 地 | STM32 GND | Zynq GND |
| DAC 时钟 | Zynq PL T4 | DAC904 CLK |
| DAC 数据 | Zynq PL V4-V7, U5-U12, Y5-Y11 | DAC904 D[13:0] |
| 50MHz 晶振 | 板载 | Zynq PL Y9 |

---

## Block Design 架构

```
ZYNQ7 PS                   
├ UART1 (MIO 8-9, 115200)  
├ M_AXI_GP0 ──→ AXI Interconnect (1M/7S)
│                  ├ M00 → axi_gpio_ctrl  (0x41200000)
│                  ├ M01 → axi_gpio_fcw   (0x41220000)
│                  ├ M02 → axi_gpio_mod   (0x41240000)
│                  ├ M03 → axi_gpio_delay (0x41250000)
│                  ├ M04 → axi_gpio_atten (0x41260000)
│                  ├ M05 → axi_gpio_phase (0x41210000)
│                  └ M06 → axi_gpio_amp   (0x41230000)
│                              │
│                         7×32-bit GPIO outputs
│                              │
│                              ▼
│                        sig_gen_top (RTL)
│                              │
├ FCLK_CLK0 (50MHz)           │
└──────────────────────────────┘
         │
    clk_wiz (50→125MHz) ──→ sig_gen_top/clk_125m
         │
    proc_sys_reset ×2 (AXI域 + 信号链域)
```

**BD 组件**：
- ZYNQ7 Processing System
- Clocking Wizard (50M→125M)
- Processor System Reset ×2
- AXI Interconnect (1 Master, 7 Slaves)
- AXI GPIO ×7 (32-bit, All Outputs)
- xlconstant (复位释放)
- sig_gen_top (RTL Module)
- ILA (调试用)

---

## 通信协议 (STM32 → Zynq)

### 物理层
- 接口：UART，单向（STM32 TX → Zynq RX）
- 波特率：115200，8N1
- 电平：3.3V LVCMOS，直连无需转换

### 帧格式
```
$F035A0500M50D110P000L10TAMO0*XX\r\n
```

| 字段 | 含义 | 格式 | 示例 |
|------|------|------|------|
| `$` | 帧头 | — | `$` |
| `Fxxx` | 载波频率 (MHz) | 3位数字 | `F035`=35MHz |
| `Axxxx` | 直达信号幅度 (mV) | 4位数字 | `A0500`=500mV |
| `Mxx` | 调制度 (%) | 2位数字 | `M50`=50% |
| `Dxxx` | 多径时延 (ns) | 3位数字 | `D110`=110ns |
| `Pxxx` | 多径初相 (°) | 3位数字 | `P000`=0° |
| `Lxx` | 多径衰减 (dB) | 2位数字 | `L10`=10dB |
| `Txx` | 调制类型 | 2字符 | `TAM`/`TCW` |
| `Ox` | 输出选择 | 1位数字 | `O0`=SD, `O1`=SM, `O2`=SOut |
| `*` | CRC 分隔符 | — | `*` |
| `XX` | CRC8 (HEX) | 2字符 | `C5` |
| `\r\n` | 帧尾 | — | — |

### CRC8 参数
- 多项式：0x31 (MAXIM/Dallas 1-Wire)
- 初始值：0x00
- 计算范围：`$`之后、`*`之前的所有字节

---

## PL 信号链路 (sig_gen_top)

```
clk_125m, rst_n
      │
      ├──→ DDS_carrier_SD (无相移) ──→ carrier_sd
      ├──→ DDS_carrier_SM (有初相) ──→ carrier_sm
      └──→ DDS_mod (2MHz) ──→ delay_line ──→ message_delayed
                                      │
              ┌───────────────────────┤
              ▼                       ▼
      am_modulator_SD          am_modulator_SM
              │                       │
              ▼                       ▼
      atten_ctrl (幅度)        atten_ctrl (衰减)
              │                       │
              ▼                       ▼
           sd_amped               sm_attened
              │                       │
              └───────┬───────────────┘
                      ▼
              signal_combiner (饱和加法)
                      │
                      ▼
              output_mux (SD/SM/SOut)
                      │
                      ▼
              dac904_if → DAC904 (14-bit)
```

**DDS**：1024 点正弦 LUT，32-bit 相位累加器
- FCW = (f_out / 125MHz) × 2^32
- 例：35MHz → FCW = 0x47AE147B

**AM 调制**：DSP48E1 流水线，9 周期延迟
- AM(t) = (DC + m * modulation) * carrier（m = 0.3~0.9）

**DAC 码值转换**：有符号 16-bit → DAC904 14-bit
- D = 8192 - signal/4（饱和到 [0, 16383]）

---

## PS 软件架构 (main.c)

```
main()
├ 初始化 UART1 (115200, MIO8-9)
├ 写入默认参数到 AXI GPIO
│   ctrl=0x01 (AM, SD)
│   fcw=0x47AE147B (35MHz)
│   amp=0x7FFF (满幅)
│
└ while(1)
    ├ 轮询 UART1 RX FIFO
    ├ 收到 $ 重置缓冲区
    ├ 收到 \n 触发解码
    ├ strchr 定位各字段
    ├ field_val() 解析数值
    └ Xil_Out32 写 7 个 AXI GPIO
```

**AXI GPIO 地址映射**：

| 寄存器 | 地址 | 说明 |
|------|------|------|
| GPIO_CTRL | 0x41200000 | [0]=CW/AM, [2:1]=输出选择 |
| GPIO_FCW | 0x41220000 | 载波频率字 |
| GPIO_MOD | 0x41240000 | 调制度 Q15 |
| GPIO_DELAY | 0x41250000 | 时延采样点数 |
| GPIO_ATTEN | 0x41260000 | 衰减系数 Q15 |
| GPIO_PHASE | 0x41210000 | 相位偏置字 |
| GPIO_AMP | 0x41230000 | 幅度 Q15 |

---

## 开发过程关键问题

| 问题 | 原因 | 解决 |
|------|------|------|
| Block Design 综合报 131 个未约束端口 | TopModule 被 Vivado 自动设为 sig_gen_top 而非 design_1_wrapper | 手动指定 TopModule=design_1_wrapper |
| 无 DAC 输出 | clk_wiz resetn 悬空，MMCM 不锁定 | xlconstant(1) → proc_sys_reset_0/ext_reset_in |
| OOC 合成找不到子模块 | dds_gen.v 用 $readmemh 读文件，OOC 目录无 sine_lut.mem | 将 1024 点正弦 LUT 内嵌到 dds_gen.v |
| 帧解析后输出消失 | 逐字符遍历解析有未知 bug | 改用 strchr 直接定位字段，field_val 解析数值 |
| UART 接收不到数据 | SR 寄存器 bit 读错 (bit0 非 RX 空标志) | 改为检查 bit1 (REMPTY) |
| 上电不自启动 | QSPI/SD 未在 PS 配置中使能 | JTAG 调试模式，FSBL 初始化 |

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `wireless_sys.xpr` | Vivado 工程 |
| `design_1.bd` | Block Design |
| `sources_1/new/sig_gen_top.v` | PL 顶层（BD 集成） |
| `sources_1/new/dds_gen.v` | DDS 正弦生成（内嵌 LUT） |
| `sources_1/new/am_modulator.v` | AM 调制（DSP48E1） |
| `sources_1/new/delay_ctrl.v` | 移位寄存器延时线 |
| `sources_1/new/atten_ctrl.v` | 衰减/幅度控制 |
| `sources_1/new/signal_combiner.v` | 饱和加法合路器 |
| `sources_1/new/dsp48_mul.v` | DSP48E1 原语乘法器 |
| `sources_1/new/dac904_if.v` | DAC904 接口驱动 |
| `constraints/wireless_bd_pins.xdc` | 引脚约束 |
| `ps/main.c` | PS 端主程序 |
| STM32 工程 | CubeMX HAL + FreeRTOS，OLED/按键/UART |

---

## 操作流程

1. Zynq 上电（JTAG 烧录 bitstream + PS 程序）
2. PS 初始化 → 写默认参数 → DAC 输出默认波形
3. STM32 OLED 菜单修改参数 → 自动发 ASCII 帧
4. Zynq PS 收到帧 → CRC8 校验 → 解析字段 → Xil_Out32 写 GPIO
5. PL 信号链参数更新 → DAC 输出实时变化
6. STM32 切换 O 字段可选择 SD/SM/SOut 输出

---

*文档版本: v1.0 | 2026-07-22*
